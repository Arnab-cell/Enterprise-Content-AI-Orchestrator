
version: '3'

services:

  api:
    build: .
    ports:
      - "8000:8000"

  frontend:
    image: python:3.10
    volumes:
      - .:/app
    working_dir: /app
    command: streamlit run frontend/dashboard.py
    ports:
      - "8501:8501"
